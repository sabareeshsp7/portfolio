---
// Personal portfolio hero data
const heroData = {
  title: "I'm building the future with",
  subtitle: "Code & Intelligence",
  description: "Engineering student passionate about AI/ML and software development. I create innovative solutions through machine learning, web technologies, and open-source contributions that solve real-world problems.",
  backgroundQuestion: "What describes me best?",
  backgroundOptions: [
    { id: "student", label: "I'm an Engineering Student", active: true },
    { id: "self-taught", label: "I'm Self-Taught", active: false }
  ],
  statText: "Continuously learning and building projects that combine AI/ML with modern web technologies",
  ctaText: "Explore My Journey",
  skills: ["AI/ML Enthusiast", "Full-Stack Developer", "Open Source Contributor"]
};
---
<section class="min-h-screen bg-gray-900 flex items-center px-8 lg:px-16 pt-20" role="main" aria-label="Portfolio Hero Section">
  <div class="max-w-7xl mx-auto w-full">
    <div class="grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">      <!-- Left Side - Hero Image (First on Mobile) -->
      <div 
        class="order-1 lg:order-1 flex justify-center lg:justify-start"
        style="opacity: 0; transform: translateX(-50px);"
        data-animate="image"
        role="img"
        aria-label="Developer Portfolio Hero Image"
      >        <div class="relative group floating">
          <!-- Clean Image Container -->
          <div class="relative w-80 h-96 lg:w-96 lg:h-[500px] rounded-2xl overflow-hidden shadow-2xl">            <!-- Background Gradient for Loading State -->
            <div class="absolute inset-0 bg-gradient-to-br from-blue-500 via-purple-600 to-pink-500 z-0"></div>            <!-- Main Image - FRONT OF EVERYTHING -->
            <img 
              src="/sabari.png"
              alt="Developer Portfolio - Professional headshot"
              class="absolute inset-0 w-full h-full object-cover object-center transition-all duration-700 group-hover:scale-105 z-50 rounded-2xl"
              loading="eager"
              style="z-index: 100 !important; position: absolute !important; opacity: 1 !important;"
              onerror="console.log('❌ Image failed to load:', this.src); this.style.display='none'; this.parentElement.querySelector('.fallback-visible').style.display='flex';"
              onload="console.log('✅ Image loaded successfully:', this.src); this.parentElement.querySelector('.fallback-visible').style.display='none';"
            />
            
            <!-- Fallback Text (visible if image fails) -->
            <div class="fallback-visible absolute inset-0 z-10 flex items-center justify-center text-white text-2xl font-bold bg-gradient-to-br from-blue-600 via-purple-600 to-pink-600">
              <div class="text-center">
                <div class="text-6xl mb-4">👨‍💻</div>
                <div class="text-xl">Sabari</div>
                <div class="text-sm text-gray-200 mt-2">AI/ML Engineer</div>
              </div>
            </div>
            
            <!-- Animated Border (behind image) -->
            <div class="absolute inset-0 z-5 rounded-2xl border-4 border-transparent bg-gradient-to-br from-blue-400 via-purple-500 to-pink-400 bg-clip-border animate-border-spin pointer-events-none"></div>
          </div>
          
          <!-- External Decorative Elements -->
          <div class="absolute -top-6 -right-6 w-24 h-24 bg-gradient-to-br from-cyan-400 to-blue-500 rounded-full opacity-20 blur-2xl animate-pulse group-hover:opacity-30 transition-opacity duration-700"></div>
          <div class="absolute -bottom-6 -left-6 w-20 h-20 bg-gradient-to-br from-purple-400 to-pink-500 rounded-full opacity-20 blur-xl animate-pulse delay-1000 group-hover:opacity-30 transition-opacity duration-700"></div>
          <div class="absolute top-1/3 -left-8 w-16 h-16 bg-gradient-to-br from-pink-400 to-orange-400 rounded-full opacity-15 blur-lg animate-pulse delay-500 group-hover:opacity-25 transition-opacity duration-700"></div>
          
          <!-- Interactive Glow Effect -->
          <div class="absolute inset-0 rounded-2xl bg-gradient-to-br from-blue-500/20 via-purple-600/20 to-pink-500/20 opacity-0 group-hover:opacity-100 transition-opacity duration-700 blur-xl transform scale-110"></div>
        </div>
      </div>

      <!-- Right Side - Personal Brand Content (Second on Mobile) -->
      <div 
        class="order-2 lg:order-2 text-center lg:text-left"
        style="opacity: 0; transform: translateX(50px);"
        data-animate="content"
      >        <!-- Animated Three-Line Headline -->
        <div class="animated-headline-container text-center lg:text-left mb-8">
          <h1 class="font-black text-white leading-none overflow-hidden">
            <div class="animated-line opacity-0" data-line="1">
              <span class="gradient-text-1">Building code that cares</span>
            </div>
            <div class="animated-line opacity-0" data-line="2">
              <span class="gradient-text-2">systems that scale</span>
            </div>
            <div class="animated-line opacity-0" data-line="3">
              <span class="gradient-text-3">And ideas that matter</span>
            </div>
          </h1>
        </div>

        <!-- Skills Tags -->
        <div class="flex flex-wrap justify-center lg:justify-start gap-2 mb-6">
          {heroData.skills.map((skill) => (
            <span class="px-3 py-1 bg-gray-800 text-gray-300 rounded-full text-sm font-medium border border-gray-700 hover:border-purple-500 transition-colors duration-300">
              {skill}
            </span>
          ))}
        </div>        <!-- Personal Introduction -->
        <p class="text-lg md:text-xl text-gray-300 mb-12 max-w-2xl leading-relaxed">
          {heroData.description}
        </p>

        <!-- Call-to-Action Button - Centered on All Devices -->
        <div class="flex justify-center">
          <button 
            class="bg-gradient-to-r from-blue-600 via-purple-600 to-pink-600 hover:from-blue-700 hover:via-purple-700 hover:to-pink-700 text-white font-bold py-4 px-8 rounded-full text-lg md:text-xl transition-all duration-300 hover:scale-105 hover:shadow-2xl transform active:scale-95 focus:outline-none focus:ring-4 focus:ring-purple-500/50"
            style="opacity: 0; transform: translateY(20px);"
            data-animate="cta"
            onclick="openRoadmap()"
            aria-label={`${heroData.ctaText} - Open interactive roadmap`}
          >
            <span class="flex items-center gap-2">
              {heroData.ctaText}
              <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
              </svg>
            </span>
          </button>
        </div>

        <!-- Additional SEO-friendly hidden text -->
        <div class="sr-only">
          Software engineering student portfolio showcasing AI/ML projects, web development skills, and open-source contributions. Specializing in machine learning, artificial intelligence, and modern web technologies.
        </div>
      </div>
    </div>  </div>
</section>

<!-- Roadmap Modal -->
<div 
  id="roadmap-modal" 
  class="fixed inset-0 bg-black/80 backdrop-blur-sm z-50 hidden items-center justify-center p-4"
  onclick="closeRoadmap()"
  role="dialog"
  aria-labelledby="roadmap-title"
  aria-describedby="roadmap-description"
>
  <div 
    class="roadmap-content bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 p-8 rounded-2xl max-w-2xl w-full border border-gray-700 shadow-2xl"
    onclick="event.stopPropagation()"
  >
    <!-- Modal Header -->
    <div class="flex justify-between items-center mb-8">
      <div>
        <h2 id="roadmap-title" class="text-3xl font-bold text-white mb-2">
          🗺️ My Developer Journey
        </h2>
        <p id="roadmap-description" class="text-gray-400">
          Explore my path as an AI/ML enthusiast and software engineer
        </p>
      </div>
      <button 
        onclick="closeRoadmap()"
        class="text-gray-400 hover:text-white transition-colors p-2 rounded-full hover:bg-gray-700"
        aria-label="Close roadmap modal"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </button>
    </div>

    <!-- Roadmap Steps -->
    <div class="space-y-6">
      <!-- Step 1: About -->
      <div 
        class="roadmap-step flex items-center p-4 rounded-xl bg-gradient-to-r from-blue-600/20 to-purple-600/20 border border-blue-500/30 hover:border-blue-400 transition-all duration-300 cursor-pointer group"
        onclick="navigateToSection('about')"
      >
        <div class="flex-shrink-0 w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full flex items-center justify-center mr-4">
          <span class="text-white font-bold text-lg">1</span>
        </div>
        <div class="flex-grow">
          <h3 class="text-xl font-bold text-white mb-1 group-hover:text-blue-300 transition-colors">
            👨‍💻 About Me
          </h3>
          <p class="text-gray-400 text-sm">
            Learn about my background, skills, and passion for AI/ML development
          </p>
        </div>
        <svg class="w-5 h-5 text-gray-400 group-hover:text-blue-300 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
        </svg>
      </div>

      <!-- Step 2: Projects -->
      <div 
        class="roadmap-step flex items-center p-4 rounded-xl bg-gradient-to-r from-purple-600/20 to-pink-600/20 border border-purple-500/30 hover:border-purple-400 transition-all duration-300 cursor-pointer group"
        onclick="navigateToSection('projects')"
      >
        <div class="flex-shrink-0 w-12 h-12 bg-gradient-to-br from-purple-500 to-pink-600 rounded-full flex items-center justify-center mr-4">
          <span class="text-white font-bold text-lg">2</span>
        </div>
        <div class="flex-grow">
          <h3 class="text-xl font-bold text-white mb-1 group-hover:text-purple-300 transition-colors">
            🚀 My Projects
          </h3>
          <p class="text-gray-400 text-sm">
            Explore my AI/ML projects, web applications, and open-source contributions
          </p>
        </div>
        <svg class="w-5 h-5 text-gray-400 group-hover:text-purple-300 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
        </svg>
      </div>

      <!-- Step 3: Contact -->
      <div 
        class="roadmap-step flex items-center p-4 rounded-xl bg-gradient-to-r from-pink-600/20 to-orange-600/20 border border-pink-500/30 hover:border-pink-400 transition-all duration-300 cursor-pointer group"
        onclick="navigateToSection('contact')"
      >
        <div class="flex-shrink-0 w-12 h-12 bg-gradient-to-br from-pink-500 to-orange-600 rounded-full flex items-center justify-center mr-4">
          <span class="text-white font-bold text-lg">3</span>
        </div>
        <div class="flex-grow">
          <h3 class="text-xl font-bold text-white mb-1 group-hover:text-pink-300 transition-colors">
            📧 Get In Touch
          </h3>
          <p class="text-gray-400 text-sm">
            Ready to collaborate? Let's connect and build something amazing together
          </p>
        </div>
        <svg class="w-5 h-5 text-gray-400 group-hover:text-pink-300 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
        </svg>
      </div>
    </div>

    <!-- Footer -->
    <div class="mt-8 pt-6 border-t border-gray-700 text-center">
      <p class="text-gray-400 text-sm">
        ✨ Click any step to begin your journey through my portfolio
      </p>
    </div>
  </div>
</div>

<style>  /* Custom tech-focused animations */
  @keyframes float {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-10px) rotate(2deg); }
  }
  
  @keyframes pulse-glow {
    0%, 100% { 
      box-shadow: 0 0 20px rgba(147, 51, 234, 0.3);
      transform: scale(1);
    }
    50% { 
      box-shadow: 0 0 40px rgba(147, 51, 234, 0.6);
      transform: scale(1.02);
    }
  }
  
  @keyframes image-glow {
    0%, 100% { 
      box-shadow: 0 0 30px rgba(59, 130, 246, 0.4), 0 0 60px rgba(147, 51, 234, 0.2);
    }
    50% { 
      box-shadow: 0 0 50px rgba(59, 130, 246, 0.6), 0 0 80px rgba(147, 51, 234, 0.4);
    }
  }
  
  @keyframes border-spin {
    0% { 
      background: linear-gradient(0deg, #3b82f6, #8b5cf6, #ec4899);
    }
    33% { 
      background: linear-gradient(120deg, #3b82f6, #8b5cf6, #ec4899);
    }
    66% { 
      background: linear-gradient(240deg, #3b82f6, #8b5cf6, #ec4899);
    }
    100% { 
      background: linear-gradient(360deg, #3b82f6, #8b5cf6, #ec4899);
    }
  }
  /* Image container animations */
  .floating {
    animation: float 4s ease-in-out infinite;
  }
  
  /* Ensure image is always visible on top */
  img[src="/sabari.png"] {
    z-index: 100 !important;
    position: absolute !important;
    opacity: 1 !important;
    visibility: visible !important;
  }
  
  /* Enhanced glow effect */
  .group:hover .relative.w-80 {
    animation: image-glow 2s ease-in-out infinite;
  }
  .floating {
    animation: float 4s ease-in-out infinite;
  }
  
  .neural-animation {
    animation: neural-pulse 2s ease-in-out infinite;
  }
  
  /* Enhanced hover effects for buttons */
  button[data-background]:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
  }
  
  button[data-background].active {
    animation: pulse-glow 2s ease-in-out infinite;
  }
  
  /* Gradient text animation */
  .gradient-text {
    background: linear-gradient(45deg, #3b82f6, #8b5cf6, #ec4899, #3b82f6);
    background-size: 300% 300%;
    animation: gradient-shift 3s ease infinite;
  }
  
  @keyframes gradient-shift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }
  
  /* Accessibility improvements */
  @media (prefers-reduced-motion: reduce) {
    .floating,
    .neural-animation,
    .gradient-text {
      animation: none;
    }
  }
  
  /* Focus indicators */
  button:focus-visible {
    outline: 2px solid #8b5cf6;
    outline-offset: 2px;
  }

  /* Animated Three-Line Headline Styles */
  .animated-headline-container {
    margin-bottom: 3rem;
    perspective: 1000px;
  }

  .animated-line {
    display: block;
    margin-bottom: 0.5rem;
    transform: translateY(60px) rotateX(30deg);
    opacity: 0;
    animation: slideUpFade 1.2s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
  }

  .animated-line:nth-child(1) {
    animation-delay: 0.3s;
  }

  .animated-line:nth-child(2) {
    animation-delay: 0.6s;
  }

  .animated-line:nth-child(3) {
    animation-delay: 0.9s;
  }

  @keyframes slideUpFade {
    0% {
      opacity: 0;
      transform: translateY(60px) rotateX(30deg) scale(0.9);
    }
    50% {
      opacity: 0.7;
      transform: translateY(-10px) rotateX(-5deg) scale(1.02);
    }
    100% {
      opacity: 1;
      transform: translateY(0) rotateX(0deg) scale(1);
    }
  }

  /* Responsive Font Sizes - Large and Elegant */
  .animated-line {
    font-size: 2.5rem; /* 40px - Mobile */
    line-height: 0.9;
    letter-spacing: -0.02em;
  }

  @media (min-width: 640px) {
    .animated-line {
      font-size: 3.5rem; /* 56px - Small tablet */
    }
  }

  @media (min-width: 768px) {
    .animated-line {
      font-size: 4.5rem; /* 72px - Tablet */
    }
  }

  @media (min-width: 1024px) {
    .animated-line {
      font-size: 5.5rem; /* 88px - Desktop */
    }
  }

  @media (min-width: 1280px) {
    .animated-line {
      font-size: 6.5rem; /* 104px - Large desktop */
    }
  }

  @media (min-width: 1536px) {
    .animated-line {
      font-size: 7.5rem; /* 120px - XL screens */
    }
  }

  /* Beautiful Gradient Text Effects */
  .gradient-text-1 {
    background: linear-gradient(135deg, #3b82f6, #8b5cf6, #ec4899);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: gradientFlow1 4s ease infinite;
  }

  .gradient-text-2 {
    background: linear-gradient(225deg, #8b5cf6, #ec4899, #f59e0b);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: gradientFlow2 3.5s ease infinite reverse;
  }

  .gradient-text-3 {
    background: linear-gradient(315deg, #ec4899, #f59e0b, #10b981);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: gradientFlow3 4.5s ease infinite;
  }

  @keyframes gradientFlow1 {
    0%, 100% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
  }

  @keyframes gradientFlow2 {
    0%, 100% {
      background-position: 100% 0%;
    }
    50% {
      background-position: 0% 100%;
    }
  }

  @keyframes gradientFlow3 {
    0%, 100% {
      background-position: 50% 0%;
    }
    50% {
      background-position: 50% 100%;
    }
  }

  /* Hover Effects */
  .animated-line:hover span {
    transform: scale(1.05);
    transition: transform 0.3s ease;
  }

  /* Accessibility - Reduced Motion */
  @media (prefers-reduced-motion: reduce) {
    .animated-line {
      animation: none;
      opacity: 1;
      transform: none;
    }
    
    .gradient-text-1,
    .gradient-text-2,
    .gradient-text-3 {
      animation: none;
      background-size: 100% 100%;
      background-position: center;
    }
  }
</style>

<script>
  import { animate } from "motion";

  // Animate image from left
  animate(
    "[data-animate='image']",
    { opacity: [0, 1], transform: ["translateX(-50px)", "translateX(0px)"] },
    { duration: 0.8, delay: 0.2 }
  );

  // Animate content from right
  animate(
    "[data-animate='content']",
    { opacity: [0, 1], transform: ["translateX(50px)", "translateX(0px)"] },
    { duration: 0.8, delay: 0.4 }
  );

  // Animate CTA button
  animate(
    "[data-animate='cta']",
    { opacity: [0, 1], transform: ["translateY(20px)", "translateY(0px)"] },
    { duration: 0.6, delay: 0.8 }
  );  // Interactive background selection with accessibility
  const backgroundButtons = document.querySelectorAll('[data-background]');
  backgroundButtons.forEach(button => {
    button.addEventListener('click', () => {
      // Remove active class from all buttons and update aria-checked
      backgroundButtons.forEach(btn => {
        btn.classList.remove('bg-gradient-to-r', 'from-blue-600', 'to-purple-600', 'border-blue-500', 'text-white', 'shadow-lg');
        btn.classList.add('border-gray-600', 'text-gray-300');
        btn.setAttribute('aria-checked', 'false');
        const dot = btn.querySelector('.w-3');
        if (dot) {
          dot.classList.remove('bg-white');
          dot.classList.add('bg-gray-600');
        }
      });
      
      // Add active class to clicked button and update aria-checked
      button.classList.add('bg-gradient-to-r', 'from-blue-600', 'to-purple-600', 'border-blue-500', 'text-white', 'shadow-lg');
      button.classList.remove('border-gray-600', 'text-gray-300');
      button.setAttribute('aria-checked', 'true');
      const activeDot = button.querySelector('.w-3');
      if (activeDot) {
        activeDot.classList.add('bg-white');
        activeDot.classList.remove('bg-gray-600');
      }
      
      // Add animation effect
      animate(
        button,
        { scale: [1, 1.05, 1] },
        { duration: 0.3 }
      );
    });
  });
  // Add floating animation to the image
  const imageElement = document.querySelector('[data-animate="image"] > div');
  if (imageElement) {
    imageElement.classList.add('floating');
  }

  // Roadmap Modal Functionality
  function openRoadmap() {
    const modal = document.getElementById('roadmap-modal');
    if (modal) {
      modal.style.display = 'flex';
      // Animate modal in
      animate(
        '#roadmap-modal',
        { opacity: [0, 1] },
        { duration: 0.3 }
      );
      animate(
        '.roadmap-content',
        { transform: ['scale(0.8) translateY(20px)', 'scale(1) translateY(0px)'] },
        { duration: 0.4, delay: 0.1 }
      );
    }
  }

  function closeRoadmap() {
    const modal = document.getElementById('roadmap-modal');
    if (modal) {
      animate(
        '#roadmap-modal',
        { opacity: [1, 0] },
        { duration: 0.2 }
      ).finished.then(() => {
        modal.style.display = 'none';
      });
    }
  }
  function navigateToSection(sectionId: string) {
    closeRoadmap();
    setTimeout(() => {
      const section = document.getElementById(sectionId);
      if (section) {
        section.scrollIntoView({ 
          behavior: 'smooth',
          block: 'start'
        });
      }
    }, 300);
  }

  // Make functions globally available
  (window as any).openRoadmap = openRoadmap;
  (window as any).closeRoadmap = closeRoadmap;
  (window as any).navigateToSection = navigateToSection;

  // Animate the three-line headline with enhanced effects
  const headlineLines = document.querySelectorAll('[data-line]');
  headlineLines.forEach((line, index) => {
    animate(
      line,
      { 
        opacity: [0, 1], 
        transform: ['translateY(60px) rotateX(30deg) scale(0.9)', 'translateY(0px) rotateX(0deg) scale(1)']
      },
      { 
        duration: 1.2, 
        delay: 0.5 + (index * 0.3)
      }
    );
  });
</script>
